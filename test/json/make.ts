const { readTextFileSync } = Deno;

const make = (path: string): void => {

	const make_array = (array: any[]): void => {
		;
	}

	const json = readTextFileSync(path);
	const obj = JSON.parse(json);
	;
}